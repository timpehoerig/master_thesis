#include <vector>

using tclause = std::vector<int>;
using tcnf = std::vector<tclause>;
using tmodel = std::vector<int>;
using tmodels = std::vector<tmodel>;
using ivec = std::vector<int>;
using ivvec = std::vector<ivec>;
using bvec = std::vector<bool>;
