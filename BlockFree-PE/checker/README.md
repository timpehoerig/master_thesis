# checker

Use `make` to compile.

Run `./checker --help` to see how to use `checker` directly or

Run `./run_checker.sh --help` to run both `wbcp_enum` and directly `checker` on it's output.

Use `make clean` to clean up.

# NOTE

`wbcp_enum` must be compiled.
